# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

이 프로젝트는 홈앤쇼핑 임직원용 업무 도구입니다. 현재 두 개의 HTML 기반 게임으로 구성되어 있습니다:
- `dolmen_game.html`: 원시인이 공룡을 상대하는 게임 (한국어)
- `spacewar1962.html`: 1962년 고전 게임 Spacewar!를 재현한 게임

## 개발 규칙

### 언어 설정
- **모든 화면의 글자와 안내는 한국어로 작성**
- UI 텍스트, 게임 설명, 인스트럭션 모두 한국어 사용
- 코드 주석도 한국어로 작성 (필요시)

### 금액 표기
- 원화 기호: **₩** (U+20A9)
- 천 단위 콤마 적용: 예) ₩1,000,000
- JavaScript: `number.toLocaleString('ko-KR')` 사용 권장

### 색상 규칙
- 홈앤쇼핑 브랜드 톤(블루 계열)을 기본 색상으로 사용
- 주요 색상: 파란색 계열 (예: #0052CC, #1E90FF 등)
- 보조 색상: 흰색, 어두운 회색
- 주의/경고: 빨간색 (#FF4444 등)

### 코드 변경 규칙
**코드를 수정할 때는 무엇을 왜 바꿨는지 한국어 한 줄 설명을 커밋 메시지에 포함하세요:**
- 기능 추가 예: `원시인 체력 시스템 추가 — 게임 어려움도 조정`
- 버그 수정 예: `공룡 충돌 감지 로직 수정 — 부정확한 범위 판정 해결`
- 리팩토링 예: `게임 루프 구조 개선 — 가독성 향상`

### 글자체
- 기본 글자체: **바탕글** (serif 폰트)
- CSS: `font-family: 'Batang', '바탕', serif;` 또는 시스템 기본 serif
- 모노스페이스 필요시: `'Courier New', '굴림', monospace`

## 프로젝트 구조

```
hnstest/
├── dolmen_game.html      # 공룡 사냥 게임 (한국어, HTML/CSS/JS 통합)
├── spacewar1962.html     # 1962 우주전쟁 게임 (영문, Canvas 기반)
└── CLAUDE.md             # 이 문서
```

## 주요 기술 스택

- **HTML5**: 구조 및 게임 컨테이너
- **CSS3**: 스타일링 및 애니메이션 (keyframes)
- **Vanilla JavaScript**: 게임 로직 및 이벤트 처리
- Canvas API (spacewar1962.html에서만 사용)

## 실행 및 테스트

각 HTML 파일을 브라우저에서 직접 열어 실행합니다:
- `dolmen_game.html`: 원시인 조작(↑↓), 공격(SPACE)
- `spacewar1962.html`: 플레이어1(W/A/D+S+Q), 플레이어2(↑/←/→+↓+P), 시작(SPACE)

## 게임 컴포넌트 아키텍처

### dolmen_game.html
- **게임 루프**: `requestAnimationFrame()`으로 매 프레임 갱신
- **플레이어**: 원시인(caveman) - 위치 제어, 공격 판정
- **적**: 공룡(dinosaur) - 우측에서 진입, 5초 후 게임 오버 조건
- **시스템**: 점수(score), 체력(health) 관리, 게임 오버 상태
- **충돌 감지**: `rectsIntersect()` 함수로 망치-공룡 충돌 판정

### spacewar1962.html
- **게임 루프**: `requestAnimationFrame()`으로 매 프레임 갱신
- **플레이어**: 두 개의 우주선 (Needle, Wedge) - 회전, 추진력, 발사, 하이퍼스페이스
- **중력 시뮬레이션**: 중앙 별(Star)의 중력이 우주선과 어뢰에 영향
- **무기**: 어뢰(Torpedo) - 제한된 개수(31개), 생존 시간(120 프레임)
- **충돌**: 어뢰-우주선, 우주선-별, 우주선 상호 충돌 감지
- **이펙트**: 폭발 파티클 시스템

## 코드 수정 시 주의사항

1. **HTML 구조 변경**: UI 텍스트가 한국어인지 확인
2. **스타일 수정**: 색상이 홈앤쇼핑 브랜드 톤(블루)과 일치하는지 검토
3. **게임 로직 변경**: 게임 밸런스(난이도, 점수)에 미치는 영향 고려
4. **폰트 설정**: serif 폰트(바탕글) 적용 확인
5. **금액 표시 추가**: 원화(₩) 기호와 천 단위 콤마 포함 확인
